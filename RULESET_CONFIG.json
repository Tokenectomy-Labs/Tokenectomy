{
  "rulesets": [
    {
      "name": "Master Branch Protection",
      "description": "Production-grade protection for master branch with GitHub Actions CI/CD",
      "enforcement": "active",
      "bypass_actors": [],
      "conditions": {
        "ref_name": {
          "include": ["master"],
          "exclude": []
        }
      },
      "rules": {
        "creation": true,
        "update": true,
        "deletion": true,
        "required_linear_history": true,
        "required_deployments_to_succeed": false,
        "required_signatures": true,
        "pull_request_required": {
          "dismiss_stale_reviews": true,
          "require_code_owner_review": false,
          "require_last_push_approval": false,
          "required_approving_review_count": 1
        },
        "required_status_checks": [
          "CI"
        ],
        "restrict_force_pushes": true
      }
    },
    {
      "name": "Release Branch Protection",
      "description": "Production-grade protection for release branches with GitHub Actions CI/CD",
      "enforcement": "active",
      "bypass_actors": [],
      "conditions": {
        "ref_name": {
          "include": ["release/*"],
          "exclude": []
        }
      },
      "rules": {
        "creation": true,
        "update": true,
        "deletion": true,
        "required_linear_history": true,
        "required_deployments_to_succeed": false,
        "required_signatures": true,
        "pull_request_required": {
          "dismiss_stale_reviews": true,
          "require_code_owner_review": false,
          "require_last_push_approval": false,
          "required_approving_review_count": 1
        },
        "required_status_checks": [
          "CI"
        ],
        "restrict_force_pushes": true
      }
    }
  ],
  "github_actions_workflows": {
    "CI": "ci.yml - Test suite dengan matrix (ubuntu, macos, windows) + Security Audit",
    "Release Binaries": "release.yml - Build & upload binaries untuk setiap platform",
    "Publish Docker Image to GHCR": "docker-publish.yml - Push multi-arch Docker images ke GitHub Container Registry"
  },
  "notes": {
    "status_checks": "Required status check: CI (mandatory untuk PR & push)",
    "optional_checks": "Bisa tambah 'Release Binaries' & 'Publish Docker Image to GHCR' jika diperlukan strict requirement"
  }
}
